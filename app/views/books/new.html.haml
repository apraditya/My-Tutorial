.page-header
  %h1 New book
.row
  .wrapper
    #steps
      = form_for @book do |f|
        -if @book.errors.any?
          #error_explanation
            %h2= "#{pluralize(@book.errors.count, "error")} prohibited this book from being saved:"
            %ul
              - @book.errors.full_messages.each do |msg|
                %li= msg
        %fieldset.step= render 'form_step_one',   f: f
        %fieldset.step= render 'form_step_one',   f: f
        %fieldset.step= render 'form_step_one',		f: f

= link_to 'Back', books_path
